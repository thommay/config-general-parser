source 'https://rubygems.org'

gem "terminal-notifier-guard", require: RUBY_PLATFORM.include?('darwin') && "terminal-notifier-guard"

# Specify your gem's dependencies in config-general-parser.gemspec
gemspec
